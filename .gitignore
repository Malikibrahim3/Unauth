node_modules/
# Local Codex runtime dependency link; never commit the external target.
outputs/shopify-shipbob-reconciliation-20260719/node_modules
.next/
.next-*/
next-env.d.ts
.env.local
*.tsbuildinfo
.tmp_*
.DS_Store
*.log
# Anchored to repo root only — the Chrome extension's committed build lives at
# extensions/chrome/dist and must stay tracked (served by the in-app download route).
/dist/
build/
.turbo/
tests/.test-credentials.json
tests/current/.auth/
tests/support/.auth/
tests/phase1/.auth/
tests/reports/
artifacts/living-precision/
tests/utils/csv-fixtures/
synthetic-lab/outputs/

test-data/tune/
test-results/
*.jsonl
*_orders.json
*_records.json
*_ground_truth.json
report.json
final-report.md

# Claude Code local settings (may contain secrets / permission tokens)
.claude/

# Supabase CLI temp files (project-specific, not portable)
supabase/.temp/

.vercel

# Local agent/design tooling and evidence are never deployment inputs.
.codex/
.impeccable/
private/
artifacts/
references/

# Generated browser/build receipts and alternate Next output directories.
*.trace.zip
*.har

# Preserve the committed Chrome extension distribution: the runtime download
# route serves extensions/chrome/dist and it must remain tracked.

# Local backup archives
backup-*.tar.gz
