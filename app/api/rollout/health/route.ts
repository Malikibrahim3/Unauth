import {NextResponse} from 'next/server';import {createClient,createServiceClient} from '@/lib/supabase/server';import {PERMISSIONS,requirePermission} from '@/lib/permissions';import {TABLES} from '@/lib/supabase/tables';
export const dynamic='force-dynamic';
export async function GET(){const auth=createClient();const {data:{user}}=await auth.auth.getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const svc=createServiceClient();const {denied,ctx}=await requirePermission(svc,user.id,PERMISSIONS.VIEW_SETTINGS);if(denied)return denied;const checks=await Promise.all([
 ['cases',svc.from(TABLES.MERCHANT_CLAIMS).select('id',{count:'exact',head:true}).eq('merchant_id',ctx.merchantId)],['financial_summaries',svc.from(TABLES.CASE_FINANCIAL_SUMMARIES).select('support_payout_case_id',{count:'exact',head:true}).eq('merchant_id',ctx.merchantId)],['losses',svc.from(TABLES.LOSS_CASES).select('id',{count:'exact',head:true}).eq('merchant_id',ctx.merchantId)],['recoveries',svc.from(TABLES.RECOVERY_CASES).select('id',{count:'exact',head:true}).eq('merchant_id',ctx.merchantId)],['work_tasks',svc.from(TABLES.WORK_TASKS).select('id',{count:'exact',head:true}).eq('merchant_id',ctx.merchantId)],['notifications',svc.from(TABLES.NOTIFICATIONS).select('id',{count:'exact',head:true}).eq('merchant_id',ctx.merchantId)],['connections',svc.from(TABLES.MERCHANT_INTEGRATIONS).select('id',{count:'exact',head:true}).eq('merchant_id',ctx.merchantId)]
 ] as const);const metrics=Object.fromEntries(checks.map(([name,result])=>[name,{ok:!result.error,count:result.count??null,error:result.error?.code??null}]));const failed=Object.values(metrics).filter(x=>!x.ok).length;return NextResponse.json({status:failed?'degraded':'healthy',merchantId:ctx.merchantId,checkedAt:new Date().toISOString(),failedChecks:failed,metrics},{status:failed?503:200});}
