import type { CSSProperties } from 'react';

export const IDENTITY_VARIANT_BADGE_STYLE: CSSProperties = {
  display: 'inline-flex',
  height: 18,
  alignItems: 'center',
  padding: '0 7px',
  borderRadius: 3,
  background: 'var(--sev-neutral-fill)',
  color: 'var(--sev-neutral)',
  border: '1px solid var(--surface-border)',
  fontSize: 12,
  fontWeight: 600,
};
