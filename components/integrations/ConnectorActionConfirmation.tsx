'use client';

export function ConnectorActionConfirmation({ preview, onConfirm, onCancel, busy }: { preview: { provider: string; account: string; capabilityId: string; externalRecordId: string; availability: string; reason: string; risk: string; reversible: boolean }; onConfirm: () => void; onCancel: () => void; busy?: boolean }) {
  return <div role="dialog" aria-modal="true" aria-label="Confirm source action" className="rounded-lg border bg-white p-4 shadow-xl"><h3 className="text-sm font-semibold">Confirm source action</h3><dl className="mt-3 grid grid-cols-2 gap-2 text-xs"><dt>Destination</dt><dd>{preview.provider} · {preview.account}</dd><dt>Action</dt><dd>{preview.capabilityId}</dd><dt>Record</dt><dd>{preview.externalRecordId}</dd><dt>Availability</dt><dd>{preview.availability}</dd><dt>Risk</dt><dd>{preview.risk}</dd><dt>Reversible</dt><dd>{preview.reversible ? 'Yes' : 'No'}</dd></dl><p className="mt-3 text-xs" style={{ color: 'var(--text-secondary)' }}>{preview.reason}</p><div className="mt-4 flex gap-2"><button type="button" onClick={onConfirm} disabled={busy || preview.risk === 'high'} className="rounded-md px-3 py-2 text-sm" style={{ background: 'var(--accent)', color: 'white' }}>{busy ? 'Running…' : preview.availability === 'enabled' ? 'Run action' : 'Record manual action'}</button><button type="button" onClick={onCancel} className="rounded-md border px-3 py-2 text-sm">Cancel</button></div></div>;
}
