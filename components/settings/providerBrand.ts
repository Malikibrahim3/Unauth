export const PROVIDER_BRAND_COLOURS = {
  gorgias: 'var(--accent)',
  zendesk: 'var(--text-primary)',
  freshdesk: 'var(--success)',
  woocommerce: 'var(--privacy-ink)',
  bigcommerce: 'var(--text-secondary)',
} as const;
