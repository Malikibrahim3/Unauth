# Superseded — chart variety, integrity, and geometry consistency

This document no longer defines the authenticated (`.ua-app`) chart system.

The complete replacement contract is
[`IMPL_authenticated_execution_ledger.md`](IMPL_authenticated_execution_ledger.md).

Do not recover chart palette, geometry, component variants, or migration
rules from this file's source-control history.
