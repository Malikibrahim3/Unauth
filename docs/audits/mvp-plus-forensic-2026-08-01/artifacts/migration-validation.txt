SANITIZED MIGRATION EVIDENCE — 2026-08-01

Local schema preflight: PASS
  6 required relations, 40 required columns, 3 foreign keys, 7 grant sets.

Safe disposable-database replay: PASS
  21 canonical migrations applied to a unique temporary database; schema-only
  result: 143 tables, 2 views, 90 functions, 161 policies.
  pg_cron was stubbed in-memory for the replay because the local environment
  does not provide the extension. The existing local database was not reset.

Repair migration dry validation: PASS
  The new investigation schema repair was executed in a rollback-only local
  transaction and rolled back. It was not applied to the remote target.

Canonical `supabase db reset --local` and rollout rehearsal: NOT RUN
  Both workflows can mutate or reset the existing local database and were not
  authorized for this audit.
