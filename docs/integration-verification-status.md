# Integration Verification Status

Last updated: 2026-06-06.

Safety scope: this pass used `.env.local`, local `next dev`, the configured test Shopify store, the configured test Gorgias account, and disposable `E2E_UNAUTH_TEST_SUPPORT_UI_*` Supabase rows only. No production customer data, production merchants, production billing, or live Stripe mode was used.

| Integration | Status | What was tested | What was not tested | Credentials/scopes required | Evidence | Cleanup performed | Remaining risks |
|---|---|---|---|---|---|---|---|
| Shopify | Partially live verified | Real Admin API read path to `unauth-test.myshopify.com`; real `/admin/oauth/access_scopes.json` scope discovery; latest local app `/api/shopify/install` redirect construction; mocked OAuth callback, webhook ingest, connection status, normalization, profile linking, and audit bridge regressions. | Real OAuth install/reinstall/callback with a dev app; real disposable customer/order/fulfillment creation; live webhook registration; live disconnect/reconnect; browser UI connected-state pass; Merchant A/B live Shopify isolation. | Present: `SHOPIFY_ADMIN_API_TOKEN`, `SHOPIFY_STORE_DOMAIN`, `SHOPIFY_API_KEY`, `SHOPIFY_API_SECRET`, `SHOPIFY_WEBHOOK_SECRET`. Missing for full write/webhook pass: `write_customers`, `write_orders`, `write_fulfillments` or `write_merchant_managed_fulfillment_orders`, and likely `write_webhooks`. The install route currently requests only `read_orders,read_all_orders,read_customers`. | `node ... Shopify scope probe` returned HTTP 200 for shop/scopes with read scopes only; `curl -I http://localhost:3000/api/shopify/install?...` returned 307 to Shopify OAuth; `npx jest tests/api/shopifyOAuth.test.ts ... tests/lib/shopifyOrderToCsvRow.test.ts --runInBand` passed 65 tests; `npx jest tests/lib/shopifyWebhookRegistration.test.ts --runInBand` passed. | No Shopify records created. No Shopify cleanup needed. | Full OAuth/write/webhook behavior remains blocked until the dev app requests and receives write/webhook scopes and its callback URL is set to the intended public latest-code URL. |
| Gorgias | Live verified previously; smoke regression passed | Real Gorgias API account connectivity; E2E preflight against real Gorgias, Shopify read API, Supabase, public webhook URL, and local latest-code ingest URL; support UI browser regression through latest local app and disposable Supabase data. | Full destructive Gorgias E2E rerun was intentionally skipped because current `E2E_MERCHANT_ID` has no active Gorgias connection and Scenario 1 would create/rotate a connection. | Present: `GORGIAS_BASE_URL`, `GORGIAS_API_EMAIL`, `GORGIAS_API_TOKEN`, `GORGIAS_SUPPORT_WEBHOOK_SECRET`. Full rerun requires a confirmed disposable merchant connection. | `node ... Gorgias /api/account probe` returned 200 for `unauth.gorgias.com`; `npx tsx scripts/e2e/preflight.ts` passed; `npx playwright test --config=tests/support/playwright.support.config.ts` passed 2/2. | Support Playwright teardown removed 1 test auth user, merchant, profile, profile token, and `tests/support/.auth/`; post-check showed 0 prefixed users/merchants/profiles and no auth dir. | Latest full E2E not rerun in this pass; use a clearly disposable Gorgias merchant before running `scripts/e2e/runE2E.ts`. |
| Zendesk | Mock verified only; live blocked | Mocked support ingest, ticket URL normalization, backfill, account identity, helpdesk-link status, and read-model regressions. | Live connection create/disable/reconnect, test ticket creation, backfill, webhook registration/ingest, browser UI, and merchant isolation. | Missing: `ZENDESK_SUBDOMAIN`, `ZENDESK_EMAIL`, `ZENDESK_API_TOKEN`, plus a public webhook URL for webhook/live app testing. | `npx jest tests/api/supportIngest.test.ts tests/lib/zendeskBackfill.test.ts tests/lib/zendeskAccountIdentity.test.ts tests/lib/zendeskHelpdeskLinkStatus.test.ts tests/lib/supportCaseReadModel.test.ts tests/lib/supportTicketNormalizer.test.ts --runInBand` included in the 20-suite mock regression run; all passed. | No Zendesk data created. | Human-facing ticket links are mock-verified only; live Zendesk account behavior remains unverified. |
| Freshdesk | Mock verified only; live blocked | Mocked connection settings, secret rotation/disable, authenticated webhook ingest, ticket normalization, and previous auth-ordering regression. | Live connection create/disable/reconnect, test ticket creation, webhook registration/ingest, browser UI, and merchant isolation. | Missing: `FRESHDESK_DOMAIN`, `FRESHDESK_API_KEY`, plus public webhook URL for live webhook testing. | `npx jest tests/api/freshdeskSupportConnectionSettings.test.ts tests/api/freshdeskSupportWebhook.test.ts tests/lib/supportTicketNormalizer.test.ts --runInBand` included in the 20-suite mock regression run; all passed. | No Freshdesk data created. | Live Freshdesk webhook auth and ticket-link correctness remain unverified without a test account. |
| WooCommerce | Mock verified only; live blocked | Mocked connection settings, webhook ingest/idempotency, backfill scheduling path, and order CSV conversion. | Live credential validation, order/customer read path, webhook registration/ingest, browser UI, reconnect/disable, historical backfill, and merchant isolation. | Missing: `WOOCOMMERCE_STORE_URL`, `WOOCOMMERCE_CONSUMER_KEY`, `WOOCOMMERCE_CONSUMER_SECRET`. | `npx jest tests/api/woocommerceConnectionSettings.test.ts tests/api/woocommerceWebhook.test.ts tests/lib/woocommerceBackfill.test.ts tests/lib/woocommerceOrderToCsvRow.test.ts --runInBand` included in the 20-suite mock regression run; all passed. | No WooCommerce data created. | Live dev store behavior and webhook delivery remain unverified. |
| BigCommerce | Mock verified only; live blocked | Mocked connection status/settings, webhook routing/idempotency, backfill, store-hash normalization, and order CSV conversion. | Live OAuth/connection create/disable/reconnect, API credential validation, order/customer read path, webhook registration/ingest, browser UI, and merchant isolation. | Missing: `BIGCOMMERCE_STORE_HASH`, `BIGCOMMERCE_ACCESS_TOKEN`, `BIGCOMMERCE_CLIENT_ID`, `BIGCOMMERCE_CLIENT_SECRET`. | `npx jest tests/api/bigcommerceConnectionSettings.test.ts tests/api/bigcommerceWebhook.test.ts tests/lib/bigcommerceBackfill.test.ts tests/lib/bigcommerceOrderToCsvRow.test.ts tests/lib/normalizeStoreHash.test.ts --runInBand` included in the 20-suite mock regression run; all passed. | No BigCommerce data created. | Live sandbox store behavior remains unverified. |
| Stripe / billing | Mock verified only; live blocked | Billing lifecycle, activation, and tier logic unit regressions. | Stripe test-mode product/price lookup, checkout session, billing portal session, subscription webhook ingest, UI plan state, upgrade/downgrade/cancel, failed-payment handling, and billing merchant isolation. | Missing: `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_ID`/repo equivalents `STRIPE_PRICE_PRO`, `STRIPE_PRICE_GROWTH`, `STRIPE_PRICE_TOPUP`. Must be Stripe test-mode keys only. | `npx jest tests/lib/billingActivation.test.ts tests/lib/billingLifecycle.test.ts tests/lib/billingTiers.test.ts --runInBand` included in the 20-suite mock regression run; all passed. | No Stripe objects or billing rows created. | No live Stripe behavior verified. |
| Playwright support config / app support-case UI | Partially live verified | `.env.local` now loads before env-dependent app modules; support suite creates disposable auth user/merchant/customer profile/profile-view token, logs in through latest local app, verifies customer support-case UI and claim-review support context. | External helpdesk APIs are mocked inside these two UI specs; not a replacement for live Gorgias/Zendesk/Freshdesk checks. | Requires dev/test Supabase env vars and `IDENTITY_SALT`; no third-party credentials required. | `npx playwright test --config=tests/support/playwright.support.config.ts --list` listed 2 tests; `npx playwright test --config=tests/support/playwright.support.config.ts` passed 2/2. | Global teardown deleted the prefixed auth user, merchant, customer profile, profile-view token, and runtime auth files; post-check showed zero leftovers. | Browser coverage is focused on support-case presentation, not full integration flows. |
