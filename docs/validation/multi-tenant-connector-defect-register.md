# Multi-tenant connector defect register

Updated: 2026-07-14 (Europe/London)

Status values: `reproduced`, `repairing`, `fixed-awaiting-validation`, `verified`, or `accepted limitation`.

| ID | Severity | Boundary | Reproduced inventory finding | Required repair/evidence | Status |
|---|---|---|---|---|---|
| MT-001 | Critical | OAuth | Shopify and BigCommerce validate a random state cookie separately from an unsigned merchant cookie; a callback is not atomically bound to merchant, user, provider, callback and expiry. | Protected transaction state, exact-merchant role check, expiry and one-time consume tests across two merchants. | fixed-awaiting-validation |
| MT-002 | High | OAuth | ShipBob state carries merchant/user/expiry but has no database replay consume and install/callback resolve the default workspace rather than the selected workspace. | Persist/atomically consume transaction state and validate selected workspace membership before exchange. | fixed-awaiting-validation |
| MT-003 | Critical | Ownership | Shopify/BigCommerce/WooCommerce store upserts can update `merchant_id` for an existing globally unique provider store. | Claim-safe insert/update that rejects cross-merchant ownership, including race regression tests. | fixed-awaiting-validation |
| MT-004 | Critical | Ownership | Canonical integrations and helpdesk connections do not consistently prevent the same provider account being owned by two merchants. | Global provider-account ownership constraint/preflight plus application conflict handling. | fixed-awaiting-validation |
| MT-005 | High | Credentials | Credential uniqueness and helpers are `(merchant_id, provider_id)` even after `connection_id` was added. | Connection-required encrypted credential API and safe uniqueness migration. | fixed-awaiting-validation |
| MT-006 | High | Discovery | ShipBob OAuth takes `channels[0]`; the PAT flow ignores discovered accounts and creates no complete source/import lifecycle. | Explicit channel selection or truthful flow removal; selected account persisted on connection/source account. | fixed-awaiting-validation |
| MT-007 | High | Environment | UPS/FedEx keep environment only inside an encrypted provider-level blob; generic connection rows omit environment/provider account. | Persist and use environment/account per connection, including refresh and health. | fixed-awaiting-validation |
| MT-008 | High | Jobs | Active sync uniqueness and job lookup use merchant/source rather than merchant/connection; one failed job can stop a due-job loop. | Connection-scoped dedupe/lookup/credential resolution and per-job failure isolation. | verified |
| MT-009 | High | Reconciliation | Parent/order lookups can match merchant + external ID without connection/source account. | Require connection/source-account scope and validate identical IDs across connections and merchants. | verified |
| MT-010 | High | Disconnect | Generic disconnect and credential deletion target every connection for a merchant/provider. | Require selected connection or enforce a deliberate database-backed one-connection policy; validate reconnect idempotency. | fixed-awaiting-validation |
| MT-011 | High | Webhooks | Helpdesk environment/global-secret routing fallbacks may be enabled outside local development. | Make fallback impossible in production and preview; retain explicit test-only injection. | fixed-awaiting-validation |
| MT-012 | High | Tenant defaults | E2E/support scripts and a test-auth helper contain a controlled merchant identifier fallback; ShipBob provider code can read a deployment PAT. | Remove account-specific literals and production merchant-credential fallbacks; automated scan. | fixed-awaiting-validation |
| MT-013 | Medium | Health | Connection verification covers Shopify/Gorgias only; raw provider error text can be exposed through health responses. | Per-connection checks where supported and secret-safe normalized errors. | fixed-awaiting-validation |
| MT-014 | Medium | Capabilities | Catalogue capabilities are mainly manifest-level and provider rows are collapsed, so readiness is not reliably connection-specific. | Derive runtime capability/health from the selected connection and granted scopes. | verified |
| MT-015 | Medium | Onboarding | Zendesk/Freshdesk, BigCommerce/WooCommerce, and legacy registry entries do not match the primary catalogue/route claims. | Align catalogue and routes or classify them truthfully as unavailable. | accepted limitation |
| MT-016 | Medium | Test integrity | Stale AfterShip tests/documentation imply a production connector that no longer exists. | Remove/update stale claims without weakening direct-carrier coverage. | fixed-awaiting-validation |
| MT-017 | High | Webhook lifecycle | Shopify, BigCommerce, and WooCommerce handlers can resolve historical/revoked stores and merchant+provider record keys can collide when a merchant reconnects to a different store that reuses external IDs. | Reject inactive stores and scope canonical customer/order/idempotency identity to the exact store connection. | verified |
| MT-018 | Critical | Canonical ownership | Service-role writes can pair a row's `merchant_id` with another tenant's connection, source account, order, record, ingestion event, or domain event because parent FKs validate IDs only. | Add and validate composite `(parent_id, merchant_id)` foreign keys across the connector processing graph. | fixed-awaiting-validation |
| MT-019 | Critical | RLS | Legacy source orders/customers/fulfilments/disputes have no migration enabling RLS, while the exception queue grants authenticated users direct mutation outside the permission-checked API. | Enable merchant-member read/service-write RLS across legacy canonical tables and remove direct authenticated exception mutation. | fixed-awaiting-validation |
| MT-020 | High | Secret handling | Several connector callbacks, backfill logs, health fields and API error responses persist or return raw provider/database messages. | Allowlisted error categories in storage/audit/logs and static merchant-safe response copy, backed by representative redaction tests and a repository scan. | fixed-awaiting-validation |
| MT-021 | Medium | Capabilities | The catalogue status ranking omits `active`, `syncing`, `pending` and `degraded`, so newer revoked history can be selected as current and drive the wrong scopes/counts/readiness. | Rank all lifecycle states deliberately and validate different active scopes for two merchants. | verified |
| MT-022 | Critical | Deployment configuration | Vercel Production and Preview retain merchant-specific Gorgias credentials/base URL, a ShipBob PAT/environment, a public-intake merchant ID, and a stale AfterShip API key. Even if the replacement code ignores them, their presence permits hidden tenant coupling and expands secret exposure. | Deploy code that resolves merchant credentials only from encrypted connection rows, verify production, then remove every listed legacy variable from both scopes without opening or copying its value. | fixed-awaiting-validation |
| MT-023 | High | Canonical normalization | A live idempotent ShipBob sync imported five fulfilment source records but persisted no canonical fulfilments because child lookup preferred the merchant order reference while `source_orders.external_id` stores ShipBob's internal order ID. | Prefer the embedded provider order ID for same-source parent linkage, retain the merchant reference for cross-provider reconciliation, add a regression test, and rerun the controlled sync without duplicate records. | verified |

No defect is closed by inspection alone. A fixed status requires a regression test; verified requires at least two merchant contexts and the evidence label recorded in the checkpoint checklist. MT-015 is accepted only because these backends are explicitly classified as founder-assisted or unavailable in normal onboarding rather than represented as merchant-ready.
