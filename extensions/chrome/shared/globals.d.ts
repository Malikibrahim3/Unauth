// Injected by vite at build time (see vite.config.ts `define`).
// Resolves from NEXT_PUBLIC_APP_URL / VITE_UNAUTH_API_BASE so the packed
// extension targets whatever deployment built it.
declare const __UNAUTH_API_BASE__: string;
